# Root .env.example — copy to .env and fill in values
# Individual service .env files also exist in backend/ and frontend/

# Postgres superuser password (used by docker-compose to init the DB)
POSTGRES_PASSWORD_SUPERUSER=changeme_superuser
